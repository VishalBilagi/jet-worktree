appId: com.jet.worktree
productName: Jet
artifactName: Jet-${version}-${arch}.${ext}

directories:
  output: dist

mac:
  category: public.app-category.developer-tools
  identity: "-"

files:
  - out/**/*
  - package.json

extraResources:
  - from: resources/bin/
    to: bin/
    filter:
      - jet
